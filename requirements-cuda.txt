# =============================================================================
# HALO — exact-pin requirements for CUDA reproduction (single A100-80GB).
#
# This is the full `pip freeze` of the venv that produced every number in
# STATUS.md and paper/figures/. Use this file when you want byte-for-byte
# reproduction; use requirements.txt for a looser, cross-version-friendly
# install instead.
#
# Setup (Python 3.13, CUDA 12.1 driver):
#
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -U pip wheel
#   # 1) install torch from the CUDA 12.1 wheel index (NOT pypi):
#   pip install torch==2.5.1+cu121 \
#       --index-url https://download.pytorch.org/whl/cu121
#   # 2) pre-install fire 0.7.1 (overrides kvpress's stale fire<0.7 metadata —
#   #    fire 0.6 has no py3.13 wheel; 0.7.1 works at runtime):
#   pip install --no-deps "fire==0.7.1"
#   # 3) the rest from this file:
#   pip install -r requirements-cuda.txt
#   # 4) editable install of the HALO package itself:
#   pip install -e .
#
# Python: 3.13.11 (any 3.10+ should work; 3.13 is what we tested on).
# CUDA driver: 535.288.01 / CUDA 12.2 runtime — torch wheel is cu121.
# =============================================================================

# torch (already installed via the cu121 index in step 1; pinned here for the
# record but you should *not* `pip install torch==...` from pypi — that pulls
# the CPU build).
# torch==2.5.1+cu121

# ML / training / inference stack
accelerate==1.13.0
huggingface_hub==1.14.0
transformers==5.2.0
tokenizers==0.22.2
safetensors==0.7.0
hf_transfer==0.1.9
hf-xet==1.5.0
sentencepiece==0.2.1; python_version < "3.13"

# Datasets / IO
datasets==4.8.5
pyarrow==24.0.0
pandas==2.3.3

# KV-compression baselines (kvpress)
# Note: kvpress 0.5.3 declares `fire<0.7` in its metadata, but `fire 0.6.x`
# does not have Python 3.13 wheels. We deliberately install fire 0.7.1
# (which works at runtime) via `--no-deps`. The two-step incantation:
#   pip install --no-deps "fire==0.7.1"
#   pip install kvpress==0.5.3
# is reflected in the install instructions below.
kvpress==0.5.3

# RULER data preparation
wonderwords==3.0.1
html2text==2025.4.15
beautifulsoup4==4.14.3
nltk==3.9.4
tenacity==9.1.4

# Numerics / stats / plots
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0
matplotlib==3.10.9
fonttools==4.62.1
contourpy==1.3.3
cycler==0.12.1
kiwisolver==1.5.0
pillow==12.2.0
pyparsing==3.3.2

# LongBench / fuzzy matching
fuzzywuzzy==0.18.0
python-Levenshtein==0.27.3
RapidFuzz==3.14.5
Levenshtein==0.27.3
rouge==1.0.1
regex==2026.4.4

# Tests + tooling
pytest==9.0.3
pluggy==1.6.0
iniconfig==2.3.0

# CLI / misc
PyYAML==6.0.3
tqdm==4.67.3
click==8.3.3
rich==15.0.0
typer==0.25.1
typer-slim==0.24.0
shellingham==1.5.4
termcolor==3.3.0
markdown-it-py==4.2.0
mdurl==0.1.2
Pygments==2.20.0
python-dateutil==2.9.0.post0
pytz==2026.2
tzdata==2026.2
six==1.17.0
filelock==3.25.2
fsspec==2026.2.0
packaging==26.2
psutil==7.2.2
typing_extensions==4.15.0
soupsieve==2.8.3

# HTTP stack (HF + datasets transitive)
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.13.0
idna==3.13
certifi==2026.4.22
charset-normalizer==3.4.7
urllib3==2.7.0
requests==2.33.1
aiohttp==3.13.5
aiosignal==1.4.0
aiohappyeyeballs==2.6.1
attrs==26.1.0
multidict==6.7.1
yarl==1.23.0
frozenlist==1.8.0
propcache==0.4.1

# Math + jinja transitive
sympy==1.13.1
mpmath==1.3.0
networkx==3.6.1
Jinja2==3.1.6
MarkupSafe==3.0.3

# misc transitive
joblib==1.5.3
threadpoolctl==3.6.0
xxhash==3.7.0
dill==0.4.1
multiprocess==0.70.19
cachetools==5.5.2
annotated-doc==0.0.4

# === CUDA runtime (auto-installed by torch==2.5.1+cu121) ===
# Listed for completeness / debugging; do not install separately —
# `pip install torch==2.5.1+cu121` pulls these as wheels.
nvidia-cublas-cu12==12.1.3.1
nvidia-cuda-cupti-cu12==12.1.105
nvidia-cuda-nvrtc-cu12==12.1.105
nvidia-cuda-runtime-cu12==12.1.105
nvidia-cudnn-cu12==9.1.0.70
nvidia-cufft-cu12==11.0.2.54
nvidia-curand-cu12==10.3.2.106
nvidia-cusolver-cu12==11.4.5.107
nvidia-cusparse-cu12==12.1.0.106
nvidia-nccl-cu12==2.21.5
nvidia-nvjitlink-cu12==12.9.86
nvidia-nvtx-cu12==12.1.105
